eureka:
    instance:
        metadata-map:
            management.port: ${management.port:18080}
spring:
    application:
        name: pm
    cloud:
        config:
            discovery:
                enabled: true
            fail-fast: true
    profiles:
        active: default