eureka:
    instance:
        prefer-ip-address: true
spring:
    application:
        name: pm
    cloud:
        config:
            discovery:
                enabled: true
            fail-fast: true
    profiles:
        active: default